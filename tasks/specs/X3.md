# X3 — Unattended runner with bounded writes

- **Wave:** 14
- **Lane:** operations
- **Recommended model tier:** code
- **Mode:** build
- **Dependencies:** X1, X2

## Objective

Execute a permitted operation inside its declared write scope and record what it did.

## Read

- `AGENTS.md`
- `CONTEXT.md`
- `DECISIONS.md`
- `NON_GOALS.md`
- `QUALITY_BAR.md`

## Owned outputs

- `scripts/run_unattended.py`
- `tests/test_run_unattended.py`
- `handoffs/X3.md`

## Scope rule

This is a build task. Change only the owned outputs and the handoff.

## Requirements

- Call the guard first and stop on refusal; do not reimplement a precondition.
- Treat the operation's command as untrusted with respect to its own bound.
- Write nothing at all under --dry-run.
- Append exactly one valid ledger entry per non-dry run, including refusals.

## Acceptance

- [ ] A command writing outside its scope leaves nothing behind.
- [ ] All five catalogued operations refuse against the shipped records.
- [ ] The containment mechanism's limits are stated rather than overclaimed.
- [ ] `python3 scripts/validate.py --task X3 --root .` passes.
- [ ] `handoffs/X3.md` records status, validation, decisions, risks, and deferred opportunities.
- [ ] The worktree is committed and clean.

## Stop conditions

Stop and write a `BLOCKED` handoff rather than guessing when a locked decision conflicts with this task, a current claim cannot be verified, required evidence is unavailable, or completing the task requires editing an unowned path.
