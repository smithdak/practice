# E1 — Trial Practice 001 (context pack) and record a Lab

- **Wave:** 7
- **Lane:** knowledge
- **Recommended model tier:** balanced
- **Mode:** build
- **Dependencies:** A1

## Objective

Apply the context-pack method to a real recurring task inside this repository and record the result as a Lab.

## Read

- `AGENTS.md`
- `CONTEXT.md`
- `DECISIONS.md`
- `NON_GOALS.md`
- `QUALITY_BAR.md`

## Owned outputs

- `labs/002-context-pack-trial.md`
- `handoffs/E1.md`

## Scope rule

This is a build task. Change only the owned outputs and the handoff.

## Requirements

- Run the trial on this repository's own work so the evidence is real and reproducible.
- Use the LAB schema.
- Record what the run actually shows; do not round a partial result up.
- Do not change the method's `maturity` or `evidence_quality`.

## Acceptance

- [ ] The Lab validates against the LAB schema.
- [ ] Every claimed outcome is traceable to a recorded command or artifact.
- [ ] No maturity or evidence-quality field is changed by this task.
- [ ] `python3 scripts/validate.py --task E1 --root .` passes.
- [ ] `handoffs/E1.md` records status, validation, decisions, risks, and deferred opportunities.
- [ ] The worktree is committed and clean.

## Stop conditions

Stop and write a `BLOCKED` handoff rather than guessing when a locked decision conflicts with this task, a current claim cannot be verified, required evidence is unavailable, or completing the task requires editing an unowned path.
