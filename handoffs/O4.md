# O4 Handoff

## Status

COMPLETE

## Summary

Re-ran the onboarding dry run as a three-persona simulation against the
Phase 2 artifact set and recorded `reviews/ONBOARDING_DRY_RUN_V2.md`. All
personas still reach a safe first Git contribution; the new Story intake and
consent route (G4) is unreachable from every newcomer-facing surface, the
artifact taxonomy is not linked from any contribution entry point, and the
"three Practices" copy is stale against the six-file directory. Dispositioned
all four original Q003 findings: #1 persists, #2 persists as a publish-time
gate, #3 partially addressed by the O3 checklist, #4 addressed at profile
level in `buzz/agents/STEWARD.md:83-93`. Verdict: conditional yes for private
beta via direct human invitations; public invitation promotion remains
blocked.

## Files changed

- `reviews/ONBOARDING_DRY_RUN_V2.md`
- `handoffs/O4.md`

## Validation

- Command: `python3 scripts/check_links.py`
- Result: PASS — 265 markdown files, 511 link targets, 0 broken, 0 stale
  as-of dates. (Counts include parallel workers' uncommitted files; none were
  touched by this task.)
- Command: `python3 scripts/validate.py --root .`
- Result: PASS — `Validation passed.`
- No git add/commit was run; this task's hard rules forbid it and leave
  staging to the integrator.

## Decisions made

- Kept the original dry run's method (persona step tables with
  PASS (repo)/FAIL (live) codes) and added STALL / PASS (friction) codes plus
  an explicit taxonomy-and-schemas test, which Q003 deferred.
- Expanded the entry path to start at the repository root README per the task
  statement; labeled the operator-facing README finding as scope expansion
  (N6), not a regression.
- Kept the private-beta verdict separate from the public-invitation verdict,
  because `ops/INVITE_FUNNEL.md:93-95` allows direct human invitation of
  known collaborators while the public request route remains unnamed.
- Recorded parallel Wave O outputs (O1 packet, O2 script) as not-yet-present
  facts without judging them.

## Risks or unresolved questions

- The G4 gap is a disclosure risk, not a navigation nuisance: until
  `templates/INTAKE_CONSENT.md` / `templates/REDACTION_CHECKLIST.md` are
  linked from `story.yml`, `templates/STORY.md`, and the contribution
  quickstart, a real Story submitter has no consent or redaction step on the
  documented path.
- `README.md:70-72` ("three current method files") and `CONTEXT.md:60-68`
  ("First three Practice candidates") are factually stale against
  `practices/001-006`; Q-INT owns the copy resolution.
- The two original blockers (request route, launch-copy placeholders) are
  unchanged and remain owner gates; this review cannot clear them.
- All persona reactions are simulations from repository text, not user
  research; real newcomer behavior is unknown until beta data exists.

## Deferred opportunities

- Unify the three `start-here` first-post formats (ONBOARDING, self-assessment,
  canvas) into one canonical template — one-line fixes, but they touch files
  this task does not own.
- A single "community entry" section in the root README linking
  CONTRIBUTING/quickstart/onboarding for non-operator visitors.
- Taxonomy-choice help inside the Story issue form (a short "is this really a
  Story?" prompt) rather than only upstream documentation.
- Re-running this simulation after Q-INT integration to confirm the fixed
  surfaces, before public invitation promotion.
