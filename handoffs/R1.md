# R1 Handoff

## Status

COMPLETE

## Summary

Refreshed `research/BUZZ_PLATFORM_SNAPSHOT.md` against live primary sources with a new as-of date of 2026-09-01. Every existing capability claim was re-verified and kept; nothing was removed for being unsupported. One exclusion claim was corrected: issue #6116 (desktop workflow list rendering empty) is now closed via PR #6168, so the scheduled-workflows exclusion now rests solely on #5611 (still open). Added newly verified facts only where they carry a source: voice huddles with the README caveat that huddle lifecycle events are still being wired up, the `stream|forum` and `open|private` CLI enum values (confirmed in `crates/buzz-cli/src/commands/channels.rs` source), no-relay-federation, 18+ age requirement, community-owner removal authority, and the buzz-cli TESTING.md/CHANGELOG as deeper sources. Added a "Changes since 2026-08-31" section as required.

## Files changed

- `research/BUZZ_PLATFORM_SNAPSHOT.md`

## Validation

- Command: `python3 scripts/validate.py --task R1 --root .`
- Result: fails with "Unknown task R1" — expected: R-wave tasks are not yet in `tasks/manifest.json` (Phase 2 wiring is a separate authorized task; workers must not edit the manifest).
- Command: `python3 scripts/validate.py --release`
- Result: passed.

## Decisions made

- Kept the snapshot's structure and conservative posture: no capability was added to the launch-safe CLI surface, and no workflow/forum behavior was promoted despite the #6116 fix.
- Verified the `--visibility open|private` and `--type stream|forum` enums against CLI source (`cmd_create_channel`) rather than trusting README examples alone, because the snapshot's command template had only shown `open` in fetched docs.
- Stated the #6116 closure without asserting a close date (the issue page shows Closed but no date was verifiable in the fetched content).

## Risks or unresolved questions

- The buzz repository moves fast (2,562 commits at review time; PRs #4882, #5925, #5945 are open fixes for the excluded-defect issues). The exclusions could become stale quickly; recommend the same refresh cadence before the owner-operated apply.
- GitHub issue pages fetched over HTML do not always expose close dates; the handoff claims issue states ("open"/"closed") as of 2026-09-01, not timestamps.
- The CHANGELOG version headers carry no dates; the "no release since desktop v0.5.20 (2026-08-26)" claim rests on the releases/latest page.

## Deferred opportunities

- Track the three open PRs that fix excluded defects (#4882 workflow delete, #5925 forum root kinds, #5945 forum mention delivery) so the snapshot can drop those exclusions when they merge and ship.
- When A2's link checker lands, add the snapshot's source URLs to its checked set so source rot is caught automatically.
- The CLI now exposes `notes` (NIP-23 long-form notes) per buzz-cli TESTING.md; Practice has no launch use for it, so it was not added to the capability list.
