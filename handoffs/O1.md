# O1 Handoff

## Status

COMPLETE

## Summary

Refreshed [release/GATE_EVIDENCE.md](../release/GATE_EVIDENCE.md) so every
packet reflects the evidence that exists today, superseding the earlier packet
draft committed as `96e0e6c` (that draft predates the G-wave commits and the
O3/O4/O5 outputs and said so explicitly; its stale rows are now re-derived).
The file keeps the original shape: one five-part packet per owner gate and
per evidenced operating hold in [OWNER_REVIEW.md](../release/OWNER_REVIEW.md)
— what the gate needs; evidence now available with real paths and commit ids
verified via `git log`; what is still missing and why an agent cannot supply
it; a tickable human-action checklist; and a none/partial/ready-for-decision
rating derived only from the cited evidence — plus a readiness summary table
and the closing list of inputs no packet can supply. No gate or hold is
asserted cleared; all stay OPEN.

What changed relative to `96e0e6c`:

- Evidence basis updated: trials still pinned to `6f205d65…`, but the commit
  inventory now includes the whitespace fix `c2a10c0`, the E-wave commits
  (08123e5, 42869f4, 39f290c, 1e22021, df16c27), the G-wave commits (cda441a,
  7a0573c, 3ffe31e, 1f80f96), and the prior draft `96e0e6c`.
- Hold 5 (Hosted member-visible surface) now cites
  [release/HOSTED_INSPECTION.md](../release/HOSTED_INSPECTION.md) (O3) as the
  fillable post-apply inspection record, with O4's disposition (checklist
  present, fields unfilled — no executed inspection evidence yet). The packet
  no longer calls O3 "planned".
- Hold 4 (Tested-Practice evidence) now records the six-candidate index
  (G3 added Practices 004–006, proposed, no trials, commit `3ffe31e`) and O4's
  stale-"three"-copy finding (N4) as integrator-owned copy work.
- Hold 1 and Gate 5 cite O4's V2 verdict: the request-route blocker persists;
  direct human invitations keep a conditional private-beta path open.
- Hold 2 and Hold 3 cite O5's beta kit and triage policy as roles-not-names
  scaffolding; Gate 6 cites the beta agent-sponsor role.
- Hold 6 (Steward) states plainly that O2's readiness script
  (`scripts/steward_readiness_check.py`) is still absent, and cites the new
  Steward enablement gate inside HOSTED_INSPECTION.md.
- Gate 7 (launch date) stays `none` and now names O4's copy fixes as
  pre-candidate integration work; Gate 3 notes O4's operator-facing-README
  finding (N6) as a copy item, not a destination question.

## Files changed

- `release/GATE_EVIDENCE.md` (rewritten in place — the only deliverable)
- `handoffs/O1.md` (this file)

## Validation

- Command: `python3 scripts/check_links.py`
- Result: pass — `Checked 267 markdown file(s), 597 link target(s): 0 broken
  link(s), 0 stale as-of date(s).`
- Command: `python3 scripts/validate.py --root .`
- Result: pass — `Validation passed.`
- Command: `python3 scripts/validate.py --task O1 --root .`
- Result: FAIL, `Unknown task O1` — Wave O tasks are not wired into
  `tasks/manifest.json` (workers may not edit it); same condition every Phase 2
  handoff recorded. Root validation covers the deliverable.

## Decisions made

- Treated the committed `96e0e6c` packet as a superseded draft rather than
  discarding it: every packet was re-derived against current evidence, and the
  file's evidence-basis paragraph names what it supersedes so the git record
  stays traceable.
- Cited the uncommitted sibling outputs (O3 checklist, O4 review, O5 kit and
  policy) by path and said so in the evidence basis, because the tasking
  explicitly requires citing `HOSTED_INSPECTION.md`; the integrator must
  commit those files before publishing this packet, or its links dangle.
- O2's script is cited only as planned (SWARM_PHASE2_PLAN.md), never as
  landed, because it still does not exist.
- Ratings are unchanged in kind from the draft (ready-for-decision: GitHub
  destination, licenses, agent-provider selection, Tested-Practice decision;
  none: launch date; partial: the rest) because the new evidence adds
  instruments and confirms gaps without moving any decision's missing inputs.
- Kept the agent-provider rating as "ready-for-decision (selection)" and the
  launch-date packet's separation of interim evidence (executable path) from
  decision evidence (final release record, named owners).

## Risks or unresolved questions

- The packet links four files that are currently untracked
  (release/HOSTED_INSPECTION.md, reviews/ONBOARDING_DRY_RUN_V2.md,
  ops/BETA_OPS.md, .github/TRIAGE_POLICY.md). If their owning tasks' commits
  do not land before this packet is read from a clean checkout, those links
  break; the evidence-basis paragraph flags the dependency for the integrator.
- Every rating rests on single-day, single-operator Phase 2 evidence; the
  promotion packets carry the stronger overreach caveats (Q6 review pending),
  and no independent reproducer has re-executed any Lab.
- The packet deliberately does not reconcile OWNER_REVIEW.md's snapshot review
  date (2026-08-31 in its header text) with the refreshed snapshot (as-of
  2026-09-01); OWNER_REVIEW.md is not owned by this task.
- O4's G4 finding (Story intake/consent route unreachable) is a disclosure
  risk that no gate packet owns directly; it is named in Gate 7 as
  pre-candidate work and belongs to the integrator's fix list.

## Deferred opportunities

- After Q-INT lands: regenerate the Gate 7 packet with the real
  baseline/candidate commit ids and the filled release-validation record, so
  the launch-date gate moves from `none` to at least `partial`.
- A machine-readable ratings table (YAML beside the packet) so the integrator
  can diff readiness across phases instead of re-reading prose.
- A one-page "owner day-one checklist" that orders the 15 packets into the
  approval sequence of OWNER_REVIEW.md — a view, not new evidence.
- Re-check Hold 6 and add the O2 script citation once
  `scripts/steward_readiness_check.py` lands.
