# K008 Handoff

## Status

COMPLETE

## Summary

Created Module 5, “Agentic Engineering,” as a bounded, evidence-driven engineering guide. It teaches task contracts, observable acceptance criteria, decomposition and single-owner file boundaries, planning, worktree isolation, layered verification, independent review, CI evidence, handoffs, recovery, and controls for parallel agents. It connects the module’s acceptance process to the planned Agent Verification Gate Practice without presenting that unpublished Practice as available.

The module includes an explicitly illustrative K008 swarm plan. It uses the repository’s declared task boundary and ownership model to demonstrate safe decomposition and recovery, while making no claim that the swarm or its outcomes have been measured.

## Files changed

- `guides/ai-native-practitioner/05-agentic-engineering.md`
- `handoffs/K008.md`

## Validation

- Command: `git diff --check`
- Result: PASS — no whitespace errors.
- Command: `python3 scripts/validate.py --task K008 --root .`
- Result: PASS — `Validation passed.`

## Decisions made

- Treated the Agent Verification Gate as a planned dependency and linked readers to the existing Practice index, because `practices/003-verification-gate.md` is not present in this worktree.
- Used task-level acceptance criteria and an evidence matrix as the module’s central structure, so agent completion reports remain proposals until inspected and checked.
- Defined worktrees as one possible isolation mechanism rather than a required Git workflow, preserving model-, platform-, and tool-agnostic guidance.
- Used K008 as a deliberately small worked example: one author owns both the module and its handoff, with later review/integration outside the author’s acceptance claim.

## Risks or unresolved questions

- The planned `practices/003-verification-gate.md` does not yet exist in this worktree, so the guide cannot link directly to it. The embedded gate is explicitly labeled a scaffold and should be aligned to the published Practice once that artifact is available.
- This task can validate repository structure and links locally, but cannot demonstrate a real CI run or independent review; the guide therefore treats those as required delivery evidence where available, not as completed project facts.

## Deferred opportunities

- After the Verification Gate Practice is published, replace the planned-reference language with a canonical link and review the two artifacts together.
- Add repository tooling that validates Guide module contracts and task handoff sections when a task explicitly owns validation scripts.
