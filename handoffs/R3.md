# R3 Handoff

## Status

COMPLETE

## Summary

Produced `research/COMMUNITY_LANDSCAPE.md`, a factual scan of adjacent spaces where AI practitioners already gather, verified against official/community primary sources via direct fetch on 2026-09-01. Twelve spaces across six categories were verified; r/LocalLLaMA and all Discord/Slack member counts are explicitly recorded as not verified. Every factual claim carries its source URL and as-of date; self-reported figures are labeled as such. Per-community gap notes, cross-cutting observations, and the final five positioning gaps are labeled as analysis, not fact, and the report carries a scope guard tying it to `NON_GOALS.md`.

## Files changed

- `research/COMMUNITY_LANDSCAPE.md` (new)
- `handoffs/R3.md` (new)

## Validation

- Command: `python3 scripts/validate.py --task R3 --root .`
- Result: fails with `Unknown task R3` — R3 (and the rest of Wave R) is not yet appended to `tasks/manifest.json`; the Phase 2 plan's wiring note says manual fan-out precedes manifest wiring. The output files exist and are non-empty. Plain `python3 scripts/validate.py --root .` (repo-wide, no task flag) passes with `Validation passed.` — the new files introduce no broken relative links.
- Not committed per explicit task instruction ("Do NOT run git add/commit"), which overrides the default completion step.

## Decisions made

- Covered Ollama as the verified local-LLM anchor instead of r/LocalLLaMA, whose fetch returned no readable content; the subreddit is listed under "Not verified" rather than described from memory.
- Self-reported figures visible on cited pages (e.g., MLOps Community "90,000+", Latent Space "Over 198,000 subscribers", AI Tinkerers on-page member/city counts) are quoted with explicit "self-reported/displayed on the page" labels, consistent with the evidence rule: they are verifiable-from-source, not independently measured.
- The W&B "community" URL serves the product homepage; only resources verifiable from that page (courses, blog, podcast, events) are claimed, and the community surface itself is marked not verified.
- Kept all gap statements and positioning gaps explicitly labeled "(analysis)" to satisfy the R3 acceptance criterion (factual scan only; example/analysis labeling).

## Risks or unresolved questions

- Sources are single-fetch snapshots from 2026-09-01; homepages change frequently (e.g., W&B's own page announces a domain change on September 30). A Q7 fact re-audit should re-fetch before any public use of this file.
- `validate.py --task R3` cannot pass until the Wave R tasks are wired into `tasks/manifest.json` by the authorized wiring task; committed-evidence checks for R3 will also start applying then.
- Positioning gaps are hypotheses; the Q7 re-audit should treat them as claims to challenge, especially gap 4 (ladder coverage), which is the least directly evidenced by source text.

## Deferred opportunities

- A periodic (post-launch, non-scheduled) refresh of this landscape with a link checker feeding into `scripts/check_links.py` output.
- Extending the scan to communities surfaced during Q7 review (e.g., AI Engineer conference ecosystem around Latent Space; regional MLOps meetups) — not verified here.
- A practitioner-sourced "where do you already go for methods?" survey once the community has real members; this report is source-scan only and cannot answer it.
- Deeper coverage of the Transform rung's adjacent spaces (org-design and transformation communities outside AI) — out of scope for this task.
