# G1 Handoff

## Status

COMPLETE

## Summary

End-to-end editorial pass over `guides/ai-native-practitioner/**` applying R2's
`research/GUIDE_CURRENCY_AUDIT.md` corrections. Prose that hardcoded Practice
maturity now defers to each artifact's front-matter `maturity` field, so no
guide sentence goes false when a Practice is promoted. Wave E trial references
were made canonical and factual: the Guide now links the committed trial Labs
(`labs/002` → Practice 001, `labs/003` → Practice 002, `labs/004` → Practice
003) with the framing "a first recorded trial exists … a recorded trial does
not change a method's maturity; promotion is a separate human review
decision". The K008 worked example is pinned with an as-of date. No meaning,
tone, locked positioning, or maturity claim changed; no new capability claims
added.

## Files changed

- `guides/ai-native-practitioner/README.md` — 10 edits: front matter
  `version` 0.2.0→0.2.1 and `updated` 2026-09-01; Modules intro rewritten
  (maturity deferred to front matter; three trial Labs linked factually); six
  `**Methods:**` lines de-prosed (lines 95/107/119/131/143/155 pre-edit);
  review trigger extended in Maintainers; changelog entry added.
- `guides/ai-native-practitioner/CURRICULUM.md` — 6 edits: how-to-use
  paragraph defers to front matter (lines 15–17 pre-edit); four
  "Proposed method dependency/dependencies" lines replaced with "Method
  dependency … applied at its current front-matter maturity" plus first
  recorded trial links (199/248/299/350 pre-edit); one bullet added to the
  module-author maintenance contract.
- `guides/ai-native-practitioner/05-agentic-engineering.md` — 1 edit: K008
  worked example pinned "(as of 2026-09-01)" (line 224).
- `handoffs/G1.md` — this file.

(`git status` also shows `practices/README.md` modified and
`practices/004–006.md` untracked; those belong to the parallel G3 task, not G1.)

## Corrections applied from the audit (R2)

1. **Should-fix — undated maturity restatements.** README 95/107/119/131/143/155
   and CURRICULUM 15–16/199/248/299/350 rewritten in R2's proposed style ("at
   its current front-matter maturity label"); no maturity claim flipped.
2. **Should-fix — review triggers.** README Maintainers trigger now reads
   "a referenced Practice changes materially or its maturity label changes";
   the CURRICULUM maintenance contract gained "re-verify every stated maturity
   or evidence status against the linked artifact's front matter rather than
   restating a fixed label".
3. **Note — K008 worked example (05:224).** Re-verified before pinning:
   `tasks/manifest.json` K008 outputs are one module file plus
   `handoffs/K008.md`; `tasks/specs/K008.md:42–44` require the repository
   validator, a commit, and a clean worktree. Pinned "(as of 2026-09-01)".

## Validation

- `python3 scripts/validate_artifacts.py` → "Artifact validation passed (1
  guide, 6 guide modules, 4 labs, 6 practices, 1 story)." Exit 0.
- `python3 scripts/check_links.py` → "Checked 255 markdown file(s), 423 link
  target(s): 0 broken link(s), 0 stale as-of date(s)." Exit 0.
- `python3 scripts/validate.py --root .` → "Validation passed." Exit 0.
- `python3 scripts/validate.py --task G1 --root .` → fails with "Unknown task
  G1": Phase 2 tasks are not wired into `tasks/manifest.json` (same condition
  R4 recorded). Expected; workers must not edit the manifest.
- Per this task's hard rules, no `git add`/`git commit` was run; the commit
  step of the AGENTS.md completion contract is left to the integrator.

## Handoff notes for the integrator (findings outside G1's paths — not edited)

1. `practices/README.md:3–5` — undated maturity restatement ("Each currently
   has `maturity: proposed` and `evidence_quality: none` …"). Apply R2's fix
   family: defer to each artifact's front-matter `maturity` label, optionally
   dated. Caution: the file currently carries an uncommitted concurrent edit
   from another task (G3's 004–006 index links) — integrate without clobbering
   it. Note the three Wave E trial Labs now exist, so the row may also state
   factually that first recorded trials exist while maturity stays
   front-matter-controlled.
2. `labs/001-cheap-model-bounded-task.md:202` — reword per R2: "When the
   recorded price basis is per-million tokens, convert to per-token; otherwise
   compute from the recorded basis directly." Route to the labs owner/E-wave
   or apply at integration.
3. Routed by R4 (out-of-class observation): cited "reviewed 2026-08-31" dates
   predate R1's refreshed snapshot (as-of 2026-09-01) — refresh in
   `community/ONBOARDING.md`, `community/CAPABILITY_SELF_ASSESSMENT.md`,
   `ops/BUZZ_SECURITY.md`, `ops/INVITE_FUNNEL.md`, `release/OWNER_REVIEW.md`,
   and `README.md` line 51 after R1's change propagates.

## Decisions made

- Trial references are factual existence claims only ("a first recorded trial
  exists"), never outcome claims: labs/002–004 are committed (E1–E3) and each
  self-describes as the first recorded trial of its Practice while stating it
  does not change the Practice's maturity. The guide's framing mirrors that,
  keeping evidence framing intact without asserting results.
- Lab links were placed once in the README Modules intro plus at the four
  CURRICULUM method-dependency lines (replacing the flagged restatements)
  rather than repeated in every module section, per the Guide schema's
  rule against duplicating Practice/Lab content.
- The K008 pin uses 2026-09-01 (the re-verification date) instead of R2's
  suggested "2026-08", because an as-of date records when the claim was
  verified; R2's verification and mine are both 2026-09-01.
- Version bumped 0.2.0 → 0.2.1 as a PATCH (wording and link corrections; no
  path, Practice, outcome, evaluation, or scope change) with `updated:
  2026-09-01` and a changelog entry per GUIDE_SCHEMA.
- Module-file phrases such as "the proposed [Workflow Redesign method]"
  (03:219, 04:7, 05:67, 06:5) were left unchanged: R2 reported those files
  clean, and the new README:215 trigger plus the CURRICULUM maintenance bullet
  now force a guide pass when any label changes.

## Risks or unresolved questions

- If a Practice is promoted between this edit and publication, no guide
  statement becomes false — text defers to front matter — and the new
  README:215 trigger forces the maintainer pass R2 wanted.
- The "first recorded trial exists" statements depend on labs/002–004 staying
  committed; they are. `labs/001` remains `result_status: not-run` and is not
  referenced as a trial.
- The Guide remains `status: draft`; `last_verified` is not required until it
  is published (GUIDE_SCHEMA conditional field).
- Task-scoped validation for Phase 2 tasks is impossible until the manifest
  wiring task runs; `validate.py --root .` is the available gate.

## Deferred opportunities

- Linking the matching trial Lab next to each module exercise (kept out to
  avoid duplicating the Practice/Lab content the Guide schema warns against).
- A validator rule that flags prose restatements of `maturity:` values in
  guide files (e.g., "remains `maturity: proposed`", "not a tested Practice")
  would prevent this finding class from recurring; deferred as tooling outside
  this task's owned paths.
- Reconsider whether `05-agentic-engineering.md` should link
  `tasks/specs/K008.md` directly instead of pinning a date, once the public
  visibility of the `tasks/` tree is settled.
