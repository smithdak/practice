# X2 — Operation catalog, promotion record, and fail-closed guard

- **Wave:** 13
- **Lane:** operations
- **Recommended model tier:** code
- **Mode:** build
- **Dependencies:** none

## Objective

Make unattended eligibility a record a validator checks, and refuse by default when it does not hold.

## Read

- `AGENTS.md`
- `docs/CONTEXT.md`
- `docs/DECISIONS.md`
- `docs/NON_GOALS.md`
- `docs/QUALITY_BAR.md`

## Owned outputs

- `ops/autonomy/operations.yaml`
- `ops/autonomy/promotions.yaml`
- `ops/autonomy/README.md`
- `scripts/autonomy_guard.py`
- `tests/test_autonomy_guard.py`
- `swarm/handoffs/X2.md`

## Scope rule

This is a build task. Change only the owned outputs and the handoff.

## Requirements

- Catalog only operations the repository already implements; derive each command from its script.
- Ship the promotion record empty with the kill switch engaged.
- Refuse on missing, malformed, or disagreeing records; never default to permitted.
- Refuse a write scope that could reach a file governing the agent's own bounds.

## Acceptance

- [ ] With the shipped records every catalogued operation is refused.
- [ ] A promotion alone, or a released kill switch alone, is still refused.
- [ ] A catalog entry may not declare A3, so one file can never promote.
- [ ] `python3 scripts/validate.py --task X2 --root .` passes.
- [ ] `swarm/handoffs/X2.md` records status, validation, decisions, risks, and deferred opportunities.
- [ ] The worktree is committed and clean.

## Stop conditions

Stop and write a `BLOCKED` handoff rather than guessing when a locked decision conflicts with this task, a current claim cannot be verified, required evidence is unavailable, or completing the task requires editing an unowned path.
