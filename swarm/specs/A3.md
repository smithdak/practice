# A3 — IA to community.json parity test

- **Wave:** 6
- **Lane:** buzz
- **Recommended model tier:** code
- **Mode:** build
- **Dependencies:** none

## Objective

Prevent drift between the documented Buzz information architecture and the idempotent community specification.

## Read

- `AGENTS.md`
- `docs/CONTEXT.md`
- `docs/DECISIONS.md`
- `docs/NON_GOALS.md`
- `docs/QUALITY_BAR.md`

## Owned outputs

- `tests/test_buzz_parity.py`
- `swarm/handoffs/A3.md`

## Scope rule

This is a build task. Change only the owned outputs and the handoff.

## Requirements

- Compare every channel and canvas in the IA document with `buzz/community.json`.
- Fail with the exact drifted entries named.
- Run offline against committed files only.

## Acceptance

- [ ] A channel present in one source and absent from the other fails the test.
- [ ] The failure message names the drifted entries.
- [ ] The test runs offline.
- [ ] `python3 scripts/validate.py --task A3 --root .` passes.
- [ ] `swarm/handoffs/A3.md` records status, validation, decisions, risks, and deferred opportunities.
- [ ] The worktree is committed and clean.

## Stop conditions

Stop and write a `BLOCKED` handoff rather than guessing when a locked decision conflicts with this task, a current claim cannot be verified, required evidence is unavailable, or completing the task requires editing an unowned path.
