# R2 Handoff

## Status

COMPLETE

## Summary

Produced `reviews/GUIDE_CURRENCY_AUDIT.md`, a currency audit of time-sensitive claims across all 21 published artifacts in `guides/ai-native-practitioner/`, `practices/`, `labs/`, `stories/`, and `buzz/agents/`. Result: zero stale, unsourced, or vendor-specific technical claims; zero model-agnostic violations; zero unlabeled examples. 97 checkable claims examined; 6 findings (2 should-fix, 4 note), all currency-maintenance risks rather than published errors. The two should-fix findings are undated maturity restatements in the guide README, curriculum map, and practices README that will become false the moment Wave E promotes a Practice to tested; the proposed correction is to defer to front-matter labels and extend review triggers.

## Files changed

- `reviews/GUIDE_CURRENCY_AUDIT.md` (new)
- `swarm/handoffs/R2.md` (new)

## Validation

- Command: `python3 scripts/validate.py --task R2 --root .`
- Result: link/content check passes for both new files; the validator reports `Unknown task R2` because Phase 2 task IDs are not yet wired into `swarm/manifest.json` (wiring is a separate authorized task per swarm/plans/PHASE2_PLAN.md "Wiring and completion"). This worker must not edit the manifest, so the unknown-task error is expected and recorded as evidence.
- One defect in this task's own output was caught and fixed during validation: an initial draft of the audit used a bracketed placeholder pseudo-link in a proposed-correction snippet, which the validator correctly flagged as a broken relative link; replaced with plain text.
- Cross-reference checks were machine-verified against `swarm/manifest.json`, `swarm/specs/K008.md`, `buzz/community.json`, `docs/schemas/`, `templates/`, and `docs/framework/` at HEAD, as-of 2026-09-01.

## Decisions made

- Reported the corpus as launch-clean for currency: no blocker-severity finding exists, so G1 is not blocked on currency corrections.
- Treated undated maturity restatements ("remains `maturity: proposed`") as should-fix rather than note, because Wave E's promotion decisions are the expected next event and the restatements have no maintenance trigger covering a label flip.
- Listed files with zero findings in a coverage table only (per task instruction not to list claims that are fine), with verified-clean cross-references recorded so G1 does not re-litigate them.
- No git commit: task hard rules for R2 override the standard commit step; the Director/integrator handles the commit.
- Recorded the `Unknown task R2` validator error as expected wiring state rather than blocking, with the exact output in Validation; the smallest decision needed to proceed is the authorized manifest-wiring task from the Phase 2 plan.

## Risks or unresolved questions

- Findings 1–2 (guide maturity prose) become live false statements if a Practice is promoted before G1 merges; if promotion lands first, G1's fix becomes a correction instead of prevention.
- Two findings are outside G1's owned paths and need routing: `practices/README.md:3–5` (integrator or practices owner) and `labs/001-cheap-model-bounded-task.md:202` price-basis rewording (labs owner / E-wave).
- O3's hosted-surface inspection must confirm applied STEWARD membership matches `buzz/agents/STEWARD.md:109–111`; the claim is plan-consistent with `buzz/community.json` but unverifiable until the owner-operated apply.
- The clean result is conditional on the corpus staying generic; the first vendor-specific example reintroduces every audited risk class. `buzz/agents/GUIDE_MAINTAINER.md:34–40` is the standing control.

## Deferred opportunities

- A2's as-of-date linter could enforce "no undated repo-state claims" by scanning for `maturity: proposed`-style restatements in prose outside front matter; the pattern class found here is mechanically detectable.
- A "currency review" trigger type in GUIDE_SCHEMA.md's review-trigger vocabulary would make label flips a first-class review cause instead of relying on "changes materially."
- When G2 adds Note/Project/Issue/Decision templates, LIBRARIAN.md:149–151's "do not pretend a missing canonical template exists" paragraph should be updated to point at the new canonical paths.
