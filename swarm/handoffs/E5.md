# E5 Handoff

## Status

COMPLETE

## Summary

Assembled [release/PROMOTION_PACKETS.md](../../release/PROMOTION_PACKETS.md): one
decision packet per candidate (Practices 001/002/003), each with the current
front-matter state, the trial-evidence summary from Labs 002–004 (dates,
run_counts, real pass/fail numbers, observed failure modes, and explicit
not-covered lists), the corroborating single-family skill-eval evidence with
its self-graded limitation stated, a nine-criterion promotion checklist derived
from `docs/schemas/PRACTICE_SCHEMA.md` and `practices/README.md` (each
criterion met/partial/not-met with an evidence pointer), residual risks of
promoting now vs. what a second trial should cover, and the exact human
decision phrased as a question plus the front-matter/body edits a promotion
would require. Ends with the consolidated decisions-requested table
(candidate → recommendation strength → what would change it) and the explicit
non-binding note. No maturity or evidence_quality value was changed anywhere;
no practice file, Lab, or OWNER_REVIEW.md was touched.

Per-candidate results:

- Practice 001 (context pack): 4 met / 1 partial / 4 not met. Recommendation:
  promote now is defensible (moderate–strong); the one material hole is the
  self-administered independent-reader check (assembler = executor in Lab 002).
- Practice 002 (workflow redesign): 4 met / 1 partial / 4 not met.
  Recommendation: hold (moderate). The trial executed only the deterministic
  slice (classes 4 and 6, 68/68 agreement); the method's differentiating
  human-review checkpoint (W6) and AI-triage slice (W5) were designed but never
  executed, and the reference agreement is partially circular.
- Practice 003 (verification gate): 4 met / 0 partial / 5 not met.
  Recommendation: hold (strong). Its own Lab records "Against the Practice's
  own trial bar: not fully met" — one artifact type instead of two, refusal
  path never exercised, human approval null.

## Files changed

- `release/PROMOTION_PACKETS.md` (new — the only release artifact owned by this task)
- `swarm/handoffs/E5.md` (this file)

## Validation

- Command: `python3 scripts/validate.py --task E5 --root .`
- Result: `Unknown task E5` — E5 is not wired into `swarm/manifest.json` (same
  condition every Wave E handoff recorded; workers may not edit the manifest).
- Command: `python3 scripts/validate_artifacts.py --root .`
- Result: exit 0 — artifact set unchanged by this task; no front matter was
  modified.
- Command: `python3 scripts/check_links.py`
- Result: exit 0 — 0 broken links, 0 stale as-of dates (the packet's relative
  links all resolve).
- Command: `python3 scripts/validate.py --root .`
- Result: exit 0 — `Validation passed.`
- Command: `python3 -m unittest discover -s tests`
- Result: 100 tests OK.
- Worktree: only the two owned files are new; no git add/commit was run per
  task rules.

## Decisions made

- Checklist grounding: seven schema criteria (SC-1–SC-7 from the schema's
  `tested` row and evidence-quality table) plus two README/validator criteria
  (PR-1 human review; PR-2 the practice artifact itself recording the trial,
  including the validator's `labs/…` body-link requirement for `tested`). Each
  practice's own Evaluation acceptance is applied as SC-7, which is what sinks
  Practice 003.
- "Not met (artifact)" is used where the evidence exists in a Lab but the
  practice file does not yet carry it — all three Evidence sections still
  describe "a planned trial only" and are now stale. These resolve through the
  promotion edit the reviewer approves, not through this task.
- Recommendation strengths: 001 moderate–strong (promote now, record the
  independent-reader limitation); 002 moderate hold; 003 strong hold. Rationale
  and change conditions are in the consolidated table.
- Packet 1 records the post-trial whitespace fix (commit `c2a10c0`, verified in
  git log, 2026-09-01) as a repository-state note so the reviewer sees the
  item-1a escalation from Lab 002 is closed.
- All numbers are quoted from the Labs or the eval report; the commit IDs and
  the `c2a10c0` fix were verified against git. Nothing was estimated.

## Risks or unresolved questions

- All three trials share single-operator, single-day, single-commit conditions;
  no independent reproducer has re-executed any Lab. The packets state this;
  Q6's adversarial review should check whether any packet wording overreaches
  the underlying records.
- Lab 003's checker, masking policy, and exception data remain in
  `/tmp/opencode` (per E2's task rules), so its evidence is reproducible from
  the Lab's embedded source but not yet from versioned repository tooling.
- If any promotion is approved, files referencing "proposed" status outside the
  practice files — practices/README.md, release/LAUNCH_CHECKLIST.md content
  check, release/OWNER_REVIEW.md hold row, docs/CONTEXT.md candidate note — need
  owner-side updates. The packets name them but deliberately do not edit them.
- Eval corroboration is single-family (GLM), self-run/self-graded, and by the
  evals' own policy does not count toward promotion; it is labeled as such
  everywhere it appears in the packet.

## Deferred opportunities

- A machine-readable promotion checklist (YAML/JSON) derived from
  PRACTICE_SCHEMA so packets can be regenerated and diffed mechanically (pairs
  with E3's suggested machine-readable gate records).
- Task-ready second-trial briefs per candidate: cold-reader trial for 001;
  W5/W6 execution trial with an independent reference for 002; two-artifact-type
  + refusal-path trial for 003.
- Tracking the open `README.md:43` class-5 finding and the pack-revision items
  (C10/F2/F3) as preconditions for the respective second trials.
