# Q6 Handoff

## Status

COMPLETE

## Summary

Produced [reviews/EVIDENCE_REVIEW.md](../../reviews/EVIDENCE_REVIEW.md): an
adversarial review of all four Wave E evidence sets (labs 002/003/004 and the
skill eval report + results), each attacked for its most overreaching sentence,
circularity, self-grading, single-operator, timing, demonstration-vs-measurement,
and over-generalization risks, with a per-set verdict and narrowest defensible
claim. Includes five ranked findings, nine exact required wording changes
(W1–W9) addressed to the integrator/owner, and the evidence each strongest claim
would need to stand.

Headline: the records are unusually honest individually — each Lab discloses its
own limits — but the packet structure invites a convergent-independence reading
that fails, because all four sets share one operator class (one model family,
one day, one shared mutating worktree) with zero human execution or verification
anywhere in the chain. Per-set verdicts: lab 002 yes-with-caveats (for the
promotion decision, accepting assembler=executor); lab 003 only for a narrower
claim (one-sided, tuned-on-corpus null agreement); lab 004 yes-with-caveats but
only for the hold decision (prevention/approval/refusal untested); eval report
only as a self-graded smoke test (cannot corroborate anything).

Q6 also re-executed what was re-executable and reported it in the review: the
lab 003 embedded checker's sha256 matches the canonical v0.4 hash exactly and
the null result replicates at HEAD (0/0 findings on a corpus drifted 68 → 78;
unmasked sensitivity hits now 21 vs 3); lab 004's M1 (34 tests OK) and M2 (now
270 files / 597 targets, 0 broken) replicate with the predicted drift; the
SOCIAL_KIT 26-token exception substrate counts match; commits `c2a10c0` (owner
git identity) and `421ed6e` (stat matches) verified. These re-executions are
independent runs but same-operator-class — declared in the review.

Strongest findings not already self-disclosed by the records: (1) the packet's
"Corroborating evidence" framing plus consolidated recommendations implies
independence the records don't have; (2) the packet's "68/68 per-file agreement"
headline drops the Lab's own "zero available by construction" and hides the
tuning-on-scored-corpus design and the excluded held-out file; (3) lab 002's
C1–C10 checklist was scored by the operator although the pack designates the
human release maintainer as reviewer (labs/002:66 vs :337) — an unrecorded
deviation that Packet 1's SC-2 "Met" inherits; (4) Packet 3's "full gate applied
end to end" overstates a documentation-plus-recovery rehearsal; (5) the eval
headline "45/45 pass" counts unauditable self-graded events.

## Files changed

- `reviews/EVIDENCE_REVIEW.md` (new)
- `swarm/handoffs/Q6.md` (this file)

## Validation

- Command: `python3 scripts/check_links.py`
- Result: exit 0 — `Checked 272 markdown file(s), 603 link target(s): 0 broken
  link(s), 0 stale as-of date(s).`
- Command: `python3 scripts/validate.py --root .`
- Result: exit 0 — `Validation passed.`
- Note: `validate.py --task Q6` was not expected to run — Wave Q tasks are not
  wired into `swarm/manifest.json` (same condition every Wave E/G/O handoff
  recorded).

## Decisions made

- Attacked only claims traceable to the records; three candidate attacks were
  dropped after verification failed to support them (the `c2a10c0` fix is
  authored under the owner's git identity; R4's audit does contain a per-file
  clean list, so "per-file" is fair; the packet's internal hedges already cover
  several candidate overreaches). Dropped attacks are not listed as findings.
- Wording changes W1–W9 are recommendations only; Q6 edited no reviewed file.
- Q6's own same-model-family bias is declared in the review header rather than
  hidden.

## Risks or unresolved questions

- The wording fixes are addressed to Q-INT/owner; if they are not applied before
  a human reads PROMOTION_PACKETS.md, findings 2, 4, and 5 remain live
  mislead-ers at exactly the sentences a human reads first.
- Finding 3 (operator-scored checklist vs the pack's designated human reviewer)
  needs a decision: either record it as a deviation in a pack revision, or the
  human reviewer consciously accepts it as part of the promotion decision.
- All re-executions remain same-family; the review's cross-cutting finding C1 is
  unresolved until a human executes or verifies at least one record.

## Deferred opportunities

- A second-family eval run and a human severe-failure inspection per the eval
  protocol would let Q6's Set-4 verdict be revisited.
- Re-running the E2 checker at a pinned commit with a held-out file set would
  convert its agreement claim from one-sided to two-sided (requires planted
  positives).
- A machine-readable findings register (finding → file:line → required change →
  owner) so integrator tasks can diff acceptance mechanically.
